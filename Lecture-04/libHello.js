const person = {
    name: "Alice",
    weight: 68,
}

 function sayHello() {  
    console.log("Hello World");
    }

    function cube(x) {
    return x * x * x;
    }

    const add = (x, y) => x + y;

    const status = "true";
    module.exports = { person, sayHello, cube, add, status };

